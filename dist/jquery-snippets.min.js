/*!
 * jquery-snippets v0.1.0
 */
(function ($) {
  "use strict";

  // Beispiel: kleines Plugin, das einen Klick-Handler setzt und optional Text ändert
  $.fn.snippetHello = function (options) {
    var settings = $.extend(
      { message: "Hallo!", setText: false },
      options || {}
    );

    return this.each(function () {
      var $el = $(this);
      $el.on("click.snippetHello", function () {
        if (settings.setText) $el.text(settings.message);
        alert(settings.message);
      });
    });
  };
})(jQuery);